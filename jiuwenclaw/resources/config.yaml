# JiuwenClaw Configuration Template
# Do not edit this file — override values in ~/.jiuwenclaw/config/config.yaml

channels:
  web:
    enabled: true
    host: "127.0.0.1"
    port: 19000
    path: "/ws"

models:
  default:
    model_client_config:
      api_base: https://dashscope.aliyuncs.com/compatible-mode/v1
      api_key: sk-0e4cf290ecfa4136951cc9c94df744ae
      model_name: glm-5.1
      client_provider: ${MODEL_PROVIDER:-openai}
      timeout: 1800
      max_retries: 1
    model_config_obj:
      temperature: 0.95

react:
  window_round_num: 100

heartbeat:
  enabled: false
  every: 60
  task: "Check for pending items and report status."

logging:
  level: INFO
